name: Lint and Test

on:
  pull_request:
    branches:
      - "*"
    paths-ignore:
      - "**.md"
      - "docs/**"
      - "examples/**"

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.6.0
        with:
          go-version: "1.25"

      - name: Lint
        run: make lint
  test:
    name: Test
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1

      - uses: actions/setup-go@40f1582b2485089dde7abd97c1529aa768e1baff # v5.6.0
        with:
          go-version: "1.25"

      # terraform-plugin-testing looks for a Terraform CLI on PATH first and
      # only falls back to downloading+GPG-verifying a release from
      # releases.hashicorp.com if none is found. HashiCorp's old release
      # signing key has since expired, which breaks that download fallback.
      # Installing the CLI here keeps tests on the PATH lookup and avoids it.
      - uses: hashicorp/setup-terraform@dfe3c3f87815947d99a8997f908cb6525fc44e9e # v4.0.1
        with:
          terraform_wrapper: false

      - name: Test
        run: make test

      - name: Calculate Coverage
        run: |
          TOTAL_COVERAGE=$(go tool cover -func=coverage.txt | grep total | awk '{print $3}')
          echo "TOTAL_COVERAGE=$TOTAL_COVERAGE" >> $GITHUB_ENV

      - name: Post Coverage Comment
        uses: actions/github-script@d7906e4ad0b1822421a7e6a35d5ca353c962f410 # v6.4.1
        with:
          script: |
            const coverage = process.env.TOTAL_COVERAGE;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### Test Coverage\n\nTotal Coverage: **${coverage}**`
            });
